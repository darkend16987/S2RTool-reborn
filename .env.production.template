# ========================================
# S2RTool Production Environment Configuration
# ========================================
#
# Copy this file to .env and fill in your values:
#   cp .env.production.template .env
#   nano .env
#
# ========================================

# ========================================
# REQUIRED: Google Gemini API Key
# ========================================
# Get your API key from: https://makersuite.google.com/app/apikey
# This is REQUIRED for the application to work
GEMINI_API_KEY=your_gemini_api_key_here

# ========================================
# OPTIONAL: Port Configuration
# ========================================
# Default ports (only change if there's a conflict)
BACKEND_PORT=5001
FRONTEND_PORT=3001

# ========================================
# OPTIONAL: Docker Registry Configuration
# ========================================
# Only needed if using pre-built images from a registry
# Leave as default if building locally

# Docker registry URL (e.g., docker.io, ghcr.io, your-registry.com)
DOCKER_REGISTRY=docker.io

# Your Docker Hub username or organization name
DOCKER_USERNAME=yourusername

# Image version tag
VERSION=latest

# ========================================
# OPTIONAL: Application Configuration
# ========================================
# Log level: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO

# Enable debug mode (set to False in production)
DEBUG=False

# ========================================
# NOTES
# ========================================
# 1. NEVER commit .env file to git (it's in .gitignore)
# 2. Keep your GEMINI_API_KEY secure and private
# 3. For production, consider using Docker secrets instead
# 4. Port changes require restarting containers: docker-compose restart
